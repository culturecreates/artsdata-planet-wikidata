name: bclive.ca - members

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 3,9 *"

permissions:
  contents: write

jobs:
  query-wikidata:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Query Wikidata and store dump
        run: |
          curl -sS -f \
            -H "Accept: text/turtle" \
            -H "User-Agent: artsdata-planet-wikidata/1.0 (https://github.com/culturecreates/artsdata-planet-wikidata)" \
            --data-urlencode "query=$(cat sparql/bclive-ca-members.sparql)" \
            --max-time 120 \
            https://query.wikidata.org/sparql \
            -o dumps/bclive-ca-members.ttl
          test -s dumps/bclive-ca-members.ttl

      - name: Commit updated dump
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git add dumps/bclive-ca-members.ttl
          git diff --staged --quiet && exit 0

          git commit -m "Update bclive-ca-members dump [skip ci]"
          git pull --rebase origin main
          git push origin main

  artsdata-push:
    needs: query-wikidata
    runs-on: ubuntu-latest
    steps:
      - name: Call Artsdata pipeline action
        uses: culturecreates/artsdata-pipeline-action@v4
        with:
          mode: push
          artifact: bclive-ca-members
          publisher: "${{ secrets.PUBLISHER_URI_GREGORY }}"
          downloadUrl: https://raw.githubusercontent.com/culturecreates/artsdata-planet-wikidata/refs/heads/main/dumps/bclive-ca-members.ttl
